import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.gormlabenz.capmap',
  appName: 'Capmap',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
